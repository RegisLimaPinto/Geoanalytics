mineracaoanalytics.cloud {
    # API do backend
    handle /api/* {
        reverse_proxy backend:8000
    }

    # GeoServer
    handle /geoserver/* {
        reverse_proxy geoserver:8080
    }

    # Frontend React (SPA fallback)
    handle {
        reverse_proxy frontend:80
    }
}
